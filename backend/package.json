{
  "name": "studiahub-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run kill:offline && cross-env DOTENV_CONFIG_PATH=.env.dev node -r dotenv/config ./node_modules/.bin/serverless offline --stage dev",
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "dev:dev": "cross-env DOTENV_CONFIG_PATH=.env.dev node -r dotenv/config ./node_modules/.bin/serverless offline --stage dev",
    "deploy:prod": "cross-env DOTENV_CONFIG_PATH=.env.prod node -r dotenv/config ./node_modules/.bin/serverless deploy --stage prod --region us-east-1",
    "deploy:dev": "cross-env DOTENV_CONFIG_PATH=.env.dev node -r dotenv/config ./node_modules/.bin/serverless deploy --stage dev --region us-east-1",
    "db:migrate": "cross-env DOTENV_CONFIG_PATH=.env.dev node -r dotenv/config ./node_modules/.bin/serverless dynamodb start --migrate",
    "db:init:local": "cross-env SERVICE=studiahub-backend STAGE=dev AWS_REGION=us-east-1 DYNAMO_ENDPOINT=http://localhost:8000 node scripts/init-dynamo.js",
    "db:admin": "cross-env DYNAMO_ENDPOINT=http://localhost:8000 dynamodb-admin --port 8001",
    "print:dev": "cross-env DOTENV_CONFIG_PATH=.env.dev node -r dotenv/config ./node_modules/.bin/serverless print --stage dev",
    "print:prod": "cross-env DOTENV_CONFIG_PATH=.env.prod node -r dotenv/config ./node_modules/.bin/serverless print --stage prod",
    "kill:offline": "lsof -ti :4000 | xargs -r kill -9; lsof -ti :3002 | xargs -r kill -9; pkill -f \"serverless offline\" || true"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.3.0",
    "@types/pdf-parse": "^1.1.5",
    "cross-env": "^7.0.3",
    "dotenv-cli": "^7.4.2",
    "dynamodb-admin": "^5.1.3",
    "pdfkit": "^0.17.1",
    "serverless": "^3.39.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-esbuild": "^1.54.7",
    "serverless-offline": "^13.8.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.868.0",
    "@aws-sdk/client-s3": "^3.864.0",
    "@aws-sdk/client-sqs": "^3.864.0",
    "@aws-sdk/client-textract": "^3.864.0",
    "@aws-sdk/lib-dynamodb": "^3.868.0",
    "@aws-sdk/s3-request-presigner": "^3.864.0",
    "@pinecone-database/pinecone": "^6.1.2",
    "@types/node-fetch": "^2.6.13",
    "@types/uuid": "^10.0.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0",
    "mammoth": "^1.7.2",
    "node-fetch": "^3.3.2",
    "pdf-parse": "^1.1.1",
    "stripe": "^18.5.0",
    "uuid": "^11.1.0",
    "zod": "^4.1.1"
  }
}
